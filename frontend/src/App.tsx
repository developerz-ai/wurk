export default function App() {
  // Skeleton. Real SPA: right-side nav, dark default, mobile-first, i18n,
  // TanStack Query against /wurk/api/*, SSE off /wurk/api/stream, Recharts
  // for the metrics pane. See docs/idea/08-dashboard.md.
  return (
    <div>
      <h1>Wurk</h1>
      <p>Dashboard skeleton. Implementation pending.</p>
    </div>
  );
}
